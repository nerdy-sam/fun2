var EventEmitter = require('events').EventEmitter;
var inherits = require('util').inherits;
var helpwith = require('./lib/helpwith.js');
var ports = require('./lib/ports.js');

// **Fun2** is an ...
function Fun2() {
  EventEmitter.call(this);
  this._stream = stream;

  var scope = this;
};

inherits(Fun2, EventEmitter);

module.exports = Fun2;


